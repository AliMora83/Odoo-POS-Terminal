name: Generate PROJECT-SYNC.json

on:
  push:
    branches:
      - main
      - master

permissions:
  contents: write

jobs:
  generate-project-sync:
    runs-on: ubuntu-latest
    if: github.actor != 'github-actions[bot]'

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Generate PROJECT-SYNC.json
        run: |
          TIMESTAMP=$(date -u '+%Y-%m-%dT%H:%M:%SZ')
          TODAY=$(date -u '+%Y-%m-%d')
          ACTOR="${{ github.actor }}"
          COMMIT_MSG="${{ github.event.head_commit.message }}"
          SHORT_SHA="${{ github.sha }}"
          SHORT_SHA="${SHORT_SHA:0:7}"
          VERSION=$(grep -oP 'Version: \K[0-9]+\.[0-9]+\.[0-9]+' Master.md 2>/dev/null || echo "0.0.0")

          cat > PROJECT-SYNC.json << ENDJSON
          {
            "project": "Odoo-POS-Terminal",
            "repo": "AliMora83/Odoo-POS-Terminal",
            "branch": "main",
            "stack": "Python / Odoo / Raspberry Pi / Thermal Printer / RFID",
            "status": "Active",
            "priority": 2,
            "priority_label": "🟡 Priority 2 — Active Development",
            "progress_percent": 25,
            "progress_label": "Hardware drivers validated · Odoo sync in development",
            "current_phase": "Phase 2 — Odoo Integration",
            "next_step": "Implement robust XML-RPC sync engine with offline queue support",
            "blocker": null,
            "live_url": null,
            "deploy_target": "Raspberry Pi (Embedded Linux)",
            "agents": ["Antigravity"],
            "version": "$VERSION",
            "last_push": {
              "timestamp": "$TIMESTAMP",
              "actor": "$ACTOR",
              "commit_message": "$COMMIT_MSG",
              "sha": "$SHORT_SHA"
            },
            "last_updated": "$TODAY"
          }
          ENDJSON

          sed -i 's/^          //' PROJECT-SYNC.json
          echo "PROJECT-SYNC.json generated."

      - name: Commit PROJECT-SYNC.json
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git diff --quiet PROJECT-SYNC.json || (
            git add PROJECT-SYNC.json &&
            git commit -m "chore: update PROJECT-SYNC.json [skip ci]" &&
            git push
          )
